define([],function () {
    var getBiggerzIndex = (function () {
        var index = 3000;
        return function (level) {
            return level + (++index);
        }
    })();
    
    return _.inherit({
        propertys:function () {
            this.wrapper = $('body');
        }
    });
})